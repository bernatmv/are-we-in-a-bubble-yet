{
  "name": "are-we-in-a-bubble-yet",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "assets": "node scripts/generate-assets.mjs",
    "build": "astro check && astro build",
    "copy:check": "node scripts/check-copy.mjs",
    "data:check": "node scripts/check-data.mjs",
    "seo:check": "node scripts/check-seo.mjs",
    "vercel-build": "npm run update-data && npm run data:check && npm run copy:check && npm run build && npm run seo:check",
    "preview": "astro preview",
    "update-data": "node scripts/update-data.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^7.1.3",
    "sharp": "^0.34.3",
    "typescript": "^5.8.3"
  }
}
